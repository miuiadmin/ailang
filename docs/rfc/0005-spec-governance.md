# RFC 0005 · P0-0 规范治理地基—— Conformance / RFC 2119 / 行为分类 / Glossary / RFC lifecycle

| | |
|---|---|
| **状态** | 草案（Draft v1）—— 待 review（已跑多轮对抗式 workflow pass-2–pass-18，post-pass-14 全部 findings 已修正；pass-15 / pass-16 报 0 confirmed；pass-17 报 2 confirmed [1M/1L] 并已全部修正；pass-18 报 1 confirmed [1M]（D.1 MUST 类遗漏正文主导词形「须」73 处、与 pass-17 禁止类遗漏同构）并已修正；pass-19 报 4 confirmed [0H/2M/2L]（§6/§10 P0-3→P1+ 悬空引用 / §4 line82 章节号归属 / §5 line104 复合标签示例 `[语义/示例]`→`[示例/语义]` / D.1 漏禁止词「不能」5 处）并已全部修正；pass-20 报 3 confirmed [0H/0M/3L]（§4 line83 + §14 line237 把 §81「不能」误标「均 Part I 规范性强制条款」、但 §81 实为 Part IV informative——frozen-boundary / rfc2119-mapping / glossary-completeness 三维度同构命中同一根因）并已全部修正；**pass-21 报 3 confirmed [0H/1M/2L]**（selfconsistency / frozen-boundary / glossary-completeness 三维度同构命中同一根因——pass-20 修 §81 Part 归属时把余 3 处「不能」误标「同一禁止不变量」统一回指 §18.5 line848、但 §48 line2133 实为 §15.3 名义类型 / §84 #1 字面量强制（名义类型禁隐式转换不变量）、§74.2 line3021 为 §18.5 line846 borrow_mut 独占、仅 §81 line3175 为 line848 struct 字段）已全部修正（§4 line83 + §14 pass-20 note ① 按 3 处各自实际不变量分别回指 Part I 源头条款）；**pass-22 报 2 confirmed [0H/0M/2L]**（rfc2119-mapping / glossary-completeness 两维度同构命中同一根因——pass-19 对禁止类词形列表的对称修复补到了 §4 line71 表 + line83 注 + line155 D.1 三处与 §1 line22 词汇审计，但 §4 line85 摘要禁止类列举漏扩「不能」（pass-19 sync 盲点）；另 §4 line82「须」示例「§25『extern 须置于 unsafe 块』」词形错挂——冻结 §25.1 line1378 把 extern/unsafe 归入「必需操作」清单（用「必需」非「须」）、与 line85 排除「需/必需」自相矛盾）已全部修正（§4 line85 摘要禁止类列举扩入「不能」/ §4 line82「须」示例换 §25.1 真实「须」条款「全局可变须走 `Mutex<T>`/`Shared<T>`」）；**pass-23 / pass-24 / pass-25 本 RFC clean**（findings 分别落 0007 / 0006 / 0006+0007）；**pass-26 报 1 confirmed [0H/0M/1L]**（glossary-completeness——D.1 line155「完整术语映射」声称 MUST 类已覆盖「必须 / 须」全部词形、但冻结正文另有独立情态「必」4 处〔§11 line425「拼接必分配新缓冲」/ §12.2 line452「导入路径必含根包」（均 Part I 规范性）/ §84 line3259「interface 方法必带 `effects`」（[语义]）/ line3368「导入路径必含根包」（[一致性]、§12.2 复述；同行并陈情态「必」与描述「必需」可区分）〕、与 pass-18「须」/pass-19「不能」同构遗漏）已修正（§1 line22 词汇审计补「必 4 处」+ §4 line70 表 MUST 行扩「须 / 必」+ §4 line82 注补「必」示例 + §4 line85 摘要扩「必」+ §8 line155 D.1 扩「必」并修正「全部词形」声称——方案①与 pass-18 四件套对称）；目标收敛 0H/0M/0L，对齐 RFC 0001 v6 / 0002 v8 / 0003 v5 / 0004 v5）|
| **目标版本** | **v0.3+**（**不触动 v0.2.1 冻结规范**：§1–§94、56 关键字、110 决议均不变；P0-0 为规范工程治理层，优先级最高，可先于特性 RFC 合并）|
| **日期** | 2026-07-27 |
| **分级** | **P0-0**（综合判断 [`docs/research/synthesis-2026-07.md`](../research/synthesis-2026-07.md) §6 排定的第一优先级；其余一切补齐的元前提）|
| **承接** | [`synthesis-2026-07.md`](../research/synthesis-2026-07.md) §6 P0-0 + §3（两类问题：未定义地基 vs 锁死决策）+ §7（文档分≈设计分）；[`spec-maturity-2026-07.md`](../research/spec-maturity-2026-07.md) §治理三件套（conformance / RFC 2119 / normative-informative 全缺，是「约当 Rust 2010–2012」判决的关键成因；18 成熟特征仅 2 部分具备 / 0 完全具备）；[`deep-review-2026-07.md`](../research/deep-review-2026-07.md) §90 UB 立场 + 可预期维度求值顺序缺口 |

---

## 1. 动机

两轮独立深度评审（设计 5.25 / 文档 5.2）合并后的**核心洞察**：AILang 的问题分两类——**未定义的地基**（便宜、阻塞编译器）与**锁死的坏决策**（贵、需重新决议）。而所有「未定义地基」类补齐的共同前提，是一层此前两轮都漏掉排第一的东西：**规范治理地基**。

**为什么治理地基必须排第一**。没有 conformance 条款，无法判断「何为合规实现」；没有 RFC 2119 规范性词汇，连「补一条 typing rule 是 MUST 还是 SHOULD」都说不清；没有 normative / informative 分区，无法区分「实现必须满足」与「仅说明」。换言之——**治理地基是其他一切补齐的规范性约束力载体**。spec-maturity 第二轮把治理三件套（conformance / RFC 2119 / normative-informative）列为全缺，正是「约当 Rust 2010–2012 年（pre-alpha）」判决的关键成因。

**本 RFC 填补的精确位置**。v0.2.1 冻结规范有 §1–§94 + 附录 A–C 的完整章节命名空间，但缺失五块规范工程元规则：

1. **Conformance 条款**——规范从未定义「何为合规 AILang 实现」。
2. **RFC 2119 规范性词汇**——正文使用「必须 / 须 / 应该 / 可以」（全文实计：「必须」14 处（其中 Part 0 动机段 1 处）、「须」73 处（独立、非「必须」——为 MUST 类正文**主导**词形）、「必」4 处（独立情态、非「必须」两字、亦非「必需」描述义——如 §11 line425「拼接必分配新缓冲」、§12.2 line452「导入路径必含根包」、§84 决议记录 line3259「interface 方法必带 `effects`」）、「应该」6 处含「不应该」、「可以」1 处；另禁止类「不能」5 处为正文 MUST NOT 词形之一、见 §4）但无约束力等级定义。
3. **normative / informative 分区**——无法区分教程示例与强制规则（§44 约定、§45–§63 示例是否构成合规要求不明）。
4. **程序行为分类法**——§90 立场「安全代码永不 UB」存在，但 UB / 实现定义 / 未指定 三分法本身未建立（synthesis §4 交叉印证 #10）。
5. **build profile**——§90 #1 隐式使用 debug / release，但 profile 本身未定义（synthesis §4 #12）。

外加两块支撑设施：**Glossary 术语表**（资料性索引）与 **RFC lifecycle 文档**（本 RFC 之后所有变更的治理通道）。

**关键性质**：上述全部属**规范工程元规则层**，**非语言语义决断**——零新关键字、零新文法产生式、不改动 §83–§94 任一冻结决议。故可作为治理补齐**独立**合并，不与 v0.3+ 的语义特性 RFC（0001–0004）耦合。

---

## 2. 设计目标

1. **不触动冻结规范**——§1–§94 的语言语义、56 关键字、§83–§94 的 110 项可逆决议，**全部不变**。本 RFC 只新增元规则层内容（落地为 §1.5 + 附录 D + 独立 `docs/rfc-process.md`），不修改既有条款的语义。
2. **零新关键字 / 零新文法产生式**——`must`/`shall`/`should`/`may`/`conformance`/`normative`/`profile` 均**非关键字**（§9 的 56 不含其一，grep 确认）；不触动 §27 任何产生式。全部表面 = 规范正文文字 + 附录表格 + 独立文档。
3. **与既有冻结决议自洽**——本 RFC 定义的行为分类、build profile **必须**与 §90 #1（release 二补数回绕 / debug panic）、§90 #4（unsafe 精确清单 + FFI→abort）、§90 #5（字段 Drop 声明序）、§17（panic = 确定性栈展开 + 同序 Drop）完全对齐，不引入矛盾（§10 自洽核查表）。
4. **治理补齐非破坏性**——不升版本号（v0.2.1 维持）；版本演进留待 P0 路线（P0-0 → P0-4）完成时统一。
5. **最小自由度**——v0.2.1 当前**未**登记任何「实现定义行为」（目标是最小化此类自由度，对齐 Rust Reference 取向）；未来若引入须在附录登记。

---

## 3. Conformance：合规 AILang 实现

落地形态：规范新增 **§1.5.3**。

一个**合规的 AILang 实现**（conforming implementation）**必须**满足：

1. **接受并正确编译**所有符合 §27 形式文法、且**通过本规范全部规范性静态检查**（类型 §15、所有权 §18、效果 §19、契约 §20、可见性 §22 等——非穷举）的**安全代码**（safe code，即不含 `unsafe` 块的程序），并保证其行为符合本规范的规范性条款。
2. **遵守行为分类**（§6）：对安全代码，**不得**引入未定义行为。
3. **生成符合 §24 schema 的 `.ailmeta`**（随包强制产物，§32）——其字段集合遵循 §24（`.ailmeta` schema 的规范性来源），且输出**确定性**（固定字段排序）；该确定性要求由本条**确立为规范性义务**。（§78 仅为 Part IV 资料性的实现层排序描述，不构成本条义务的权威来源——见 §5。）
4. **诊断**违反规范性条款的程序（类型错误、所有权违规、可见性违规等），诊断信息**必须**结构化（**诊断发射协议**见 [RFC 0008](./0008-diagnostics-taskhandle.md) §4 的 JSON Lines 契约；§69.3 的内部 `Diagnostic` 结构为 Part IV 资料性参考，不构成本条义务的权威来源）。

> **引用纪律**：§3 的 MUST / SHOULD 条款以规范性来源（Part I（§27 / §15 / §18 / §19 / §20 / §22 / §24）、Part II §32，及 §1.5 本节、RFC 0008 §4 协议等——非穷举）为权威；Part IV 的 §69.3 / §78 仅作资料性参考被提及，不因被本节引用而升格为合规要求（与 §5 的 normative / informative 分区一致）。

实现**可以**提供超出本规范的能力（扩展），但**不得**改变安全代码的规范性语义。`unsafe` 块（§25）与 FFI（`extern`，§25.3）是合规实现**唯一允许**偏离安全语义的逃生舱，其边界由 §90 #4 精确清单锁定。

> 对齐参考：Rust Reference「Conformance」、Go spec「Program execution」、C++ [intro.compliance]。AILang 的合规定义较窄——聚焦「安全代码的规范性保证」，因为 unsafe/FFI 是显式逃生舱（§25）。

---

## 4. 规范性词汇（RFC 2119 中文映射）

落地形态：规范新增 **§1.5.1**。

本规范采用下列规范性词汇表达约束力等级，含义对齐 IETF RFC 2119 / RFC 8174：

| 中文 | RFC 2119 | 约束力 |
|---|---|---|
| **必须** / 须 / 必 | MUST / SHALL / REQUIRED | 满足方为合规；违反即不合规（「须」「必」为正文主导 MUST 词形、与「必须」同义，见下注） |
| **不得** / 禁 / 禁止 / 不可 / 不能 | SHALL NOT / MUST NOT | 禁止该行为；违反即不合规（「禁 / 禁止 / 不可 / 不能」为正文主导禁止词形、与「不得」同义，见下注） |
| **应** | SHOULD / RECOMMENDED | 强烈建议；偏离须有正当理由（对齐 RFC 2119 / 8174 SHOULD 语义） |
| **不应** | SHOULD NOT / NOT RECOMMENDED | 强烈不建议；偏离可有正当理由、不必定不合规——约束力**弱于**「不得」（绝对禁止），二者不可互替 |
| **可以** | MAY / OPTIONAL | 实现自由度；是否实现均合规 |

正文以中文词汇为准，附录 D（§8）给出完整术语映射。**仅当**上述词汇以**粗体**形式出现时，才具 RFC 2119 约束力（严格对齐 RFC 8174 的单一形式标记原则——ALL CAPS 在中文场景映射为**粗体**，以消除自然语言上下文判定的模糊；义务等级 MUST / SHOULD / MAY 由粗体关键词决定）；普通行文中的「必须」「可以」不自动升级为规范性语义。

> 设计选择（中文为主 + glossary 映射）：AILang 规范正文为中文，故正文用中文规范性词汇（必须 / 不得 / 应 / 可以），避免中英混杂；同时附录 D 给出 RFC 2119 英文映射，确保与 IETF / 编译器实现者社区的对齐无歧义。

> **正文既有非表词形的约束力保留**（消除 D.1「完整术语映射」遗漏正文主导词形的等级歧义——对称覆盖 MUST 类与禁止类）：
>
> - **MUST 类「须 / 必」**：冻结规范正文（§1–§94）实际表达 MUST 所用的主导词形为独立「须」（73 处，远多于「必须」14 处；如 §12「文件路径须与声明一致」「须两遍分析」「须 `as` 消歧」、§15「谓词须 pure」「须显式 impl `Eq`」「append 须声明 borrow_mut」、§25.1「全局可变须走 `Mutex<T>`/`Shared<T>`」等——均 Part I 规范性强制条款）与独立情态「必」（4 处，非「必须」两字、亦非已排除的「必需」描述义；如 §11 line425「拼接必分配新缓冲」、§12.2 line452「导入路径必含根包」（均 Part I 规范性）、§84 决议记录 II line3259「interface 方法必带 `effects`」（[语义] 规范性分量）、line3368「导入路径必含根包」（[一致性]、§12.2 复述）——关键 line3368 同行并陈情态「必含根包」与描述「`std.` 必需」可明确区分），均与上表「必须」同义、按 MUST 等级处理。
> - **禁止类「禁 / 禁止 / 不可 / 不能」**：冻结正文实际表达禁止所用的主导词形为「禁 / 禁止 / 不可 / 不能」（如 §12.2 禁 `import *`、§15.4 禁 `old()`、§15.7 禁止 null、§17/§21 task/actor on/server handler 禁 throw、§18.4 Move-only 不可 `copy`、§11 `&&`/`||`/`!` 不可重载等、§18.5 `borrow_mut` 独占「不能」与只读借用并存、§21.2 被 spawn 数据「不能」是 `borrow` 非逃逸借用等——均 Part I 规范性强制条款；另「不能」全文 5 处中余 3 处（§48 Part III 教程注释、§74.2 Part IV 模块设计、§81 Part IV 决议汇总）为**各自对应不同禁止不变量**的 Part III/IV 资料性复述、规范权威分别回指 Part I 源头条款——§48 line 2133「不能隐式转 UserId（名义不同）」回指 §15.3 名义类型 / §84 #1 字面量强制（**名义类型禁隐式转换不变量**、非借用族）；§74.2 line 3021「borrow_mut 独占、不能与只读借用并存」回指 §18.5 line 846（**borrow_mut 独占不变量**、与上 inline 枚举「§18.5 `borrow_mut` 独占『不能』与只读借用并存」同一 bullet）；§81 line 3175「struct 字段不能存引用」回指 §18.5 line 848（**不逃逸：禁止存入 struct 字段**）——三者分属三个不同禁止不变量、均非独立 Part I 强制条款），与上表「不得」同义、按 MUST NOT 等级处理。
>
> 上节「仅粗体方生效」规则的适用对象为**本 §4 表内规范关键词的新引入用法**；冻结正文既有的「须」「必」「禁 / 禁止 / 不可 / 不能」**不因未加粗而丧失约束力**（其规范地位由 §5 Part I normative 分区承载、非由粗体标记决定）——此切割使既有强制条款的约束力不被新框架无意抹除，让 D.1 的「完整术语映射」名副其实。（「需」46 处多为描述性「需要 / 无需」、「只能」5 处为能力限制描述，MUST/能力义混杂、不映射以免过度主张，不列入此保留范围。）

---

## 5. 规范性内容与资料性内容（normative / informative）

落地形态：规范新增 **§1.5.2**。

- **规范性（normative）**：Part I 语言规范（§7–§27）、**Part II 标准库与包生态**（§28–§43——其中 §28–§42 为规范性核心：std.core / 标准库 API 签名与包模型；§43「生态优势」等纯论述段落为资料性；与冻结目录 Part II = §28–§43 一致）、§24（`.ailmeta` schema）、§1.5（本节），以及 §83–§94 决议记录中标注 `[定义]` / `[语义]` / `[文法]` / `[schema]` / `[效果]` 的条款——合规实现**必须**满足。
- **资料性（informative）**：Part 0（§1–§6 动机与导览）、Part III（§44–§64 教程示例）、Part IV（§65–§82 编译器实现设计）、附录 A / B / C / D（D = 本 RFC 新增的术语表 Glossary，§8），以及决议记录中标注 `[示例]` / `[回归]` / `[一致性]` / `[完整性]` 的说明性条款——仅供理解，**不**构成合规要求。

**标签全集归类**（§83–§94 决议记录与正文所用的方括号标注，闭合 §3 引用纪律；复合标签采 `[A/B]` 单括号斜杠形式）：

| 标签 | 类别 | 说明 |
|---|---|---|
| `[定义]` `[语义]` | 规范性 | 锁定语言语义决断 |
| `[文法]` `[schema]` `[效果]` | 规范性 | §27 文法 / §24 `.ailmeta` schema / §19 效果系统 |
| `[示例]` `[回归]` `[一致性]` `[完整性]` | 资料性 | 示例 / 回归用例 / 一致性与完整性说明 |
| `[矛盾]` `[决断]` | **修饰类，不参与规范性判定** | `[矛盾]`=消解冲突的元记录、`[决断]`=确立某一决断——仅作过程/来源修饰；复合条目的规范性**只由上述 9 类内容标签决定**（§87 #1/#2、§88 #1、§93 #1/#2、§94 #3/#7 使用） |
| 复合标签（`[A/B]` 斜杠形，如 `[示例/语义]`、`[一致性/矛盾]`） | **任一规范性内容分量即整体规范性** | 复合判定只在 9 类内容标签间套用；`[矛盾]`/`[决断]` 分量被忽略（故 `[一致性/矛盾]` 仅按 `一致性`=资料性判定） |

> 规范性内容标签集合 `{定义, 语义, 文法, schema, 效果}` 为**闭集**——未列入该集合的标签（含 `[矛盾]`/`[决断]` 与任何新标签）一律不构成规范性分量。
- **无标签条目兜底**：§83–§94 决议记录中**未标注上述任一标签**的条目（当前为 §83 决议记录 I 的 10 项紧凑索引条目——标签体系自 §84 起引入，§83 早于该约定）视为**资料性索引**，其规范性权威由各自「=> 体现在 §X.Y」指针指向的正文条款（Part I / Part II）承载（与冻结规范 §83「本节为紧凑索引——权威表述已迁入正文」一致）。此兜底闭合标签分类对无标签条目的覆盖盲区。
- 资料性内容与规范性内容冲突时，以规范性内容为准。教程示例（§45–§63）为资料性；当示例与 Part I 规则不一致时，以 Part I 为准。

> 此分区直接回应 spec-maturity 第二轮「教程示例是否构成合规要求」的歧义，并把既有结构（Part 0/III/IV 本就是不同性质内容）形式化为规范条款。

---

## 6. 程序行为分类（定义 / 未定义 / 实现定义 / 未指定）

落地形态：规范新增 **§1.5.4**。**填补 synthesis §4 交叉印证 #10（UB 三分法未建立）。**

本规范对程序行为作如下分类。**上位分类（显式声明，消除术语碰撞）**：四类中，**定义行为**（窄义，见下首项：结果唯一确定）、**实现定义行为**、**未指定行为**三者统属**「非 UB 行为」**上位大类（均对结果施加某种约束、与 UB 互斥）；**UB** 是唯一「不施加任何约束」的类别。下文「定义行为」一词指**窄义**（四分法首项、结果唯一确定），「非 UB 行为范畴」指三者之上位——二者外延不同、勿混（对齐 ISO C/C++ 的 `well-defined behavior` 窄义 vs `defined behavior` 上位的区分惯例）。

- **定义行为（defined behavior）**：本规范完整规定结果，合规实现**必须**产生该结果。安全代码的算术（§90）、析构顺序（§18.7 / §90 #5）、错误传播（§17）等均属此类。
- **未定义行为（undefined behavior, UB）**：本规范不施加任何约束——程序进入 UB 后，实现**可以**产生任意结果（含看似正常、崩溃或错误结果），且**不**保证可观测的失败。**AILang 的立场：安全代码（safe code = 不含 `unsafe` 块的程序，§3）永不产生 UB。** （`extern` FFI 调用本身须置于 `unsafe` 块内 §25.1 ② 精确清单，故「不含 `unsafe` 块」已涵盖不含 `extern`——定义统一为单一条件。） UB 仅可能来自两类来源：① `unsafe` 块内对 §25.1 精确清单所列设施（裸指针 / `extern` fn / 标 `unsafe` 的 fn）违反其运行期操作前提条件的使用（含其触发的底层 UB，如裸指针解引用无效、调用原型不匹配的 `extern` fn 等）；注：在 `unsafe` 块**外**执行清单所列操作为**编译期错误**（非运行期 UB）；② 跨 `extern`（FFI）边界的不当交互（如 C 侧 `longjmp` 跨入，§90 #4）。**定义的逻辑结构**（消除「无约束即 UB」的反向误读）：上句「本规范不施加任何约束」为 UB 被归类后的**后果性质**（单向蕴涵：UB → 无约束），**非**「凡规范不约束者皆 UB」的成员准则；UB 的**成员准则**唯「仅来自 ①② 两类来源」（穷尽，双向：来源∈{①,②} ↔ UB）。未指定行为 / 实现定义行为同样不施加唯一结果约束，但属**非 UB 行为范畴**（允许若干合规结果之一 / 须文档化），**非** UB——故「结果不唯一」不等于 UB（资源耗尽见下注）。
- **实现定义行为（implementation-defined behavior）**：本规范要求实现**文档化**其选择，但允许实现间不同。v0.2.1 当前**未**登记任何实现定义行为（见设计目标 5）；未来若引入（如目标三元组相关的数据布局），**必须**在附录登记。
- **未指定行为（unspecified behavior）**：本规范允许若干合规结果之一，不要求实现文档化选择。当前主要未指定项：**表达式求值顺序**（实参、操作数、字面量元素的求值次序；除 `&&` / `||` 短路由语义内在规定外，未作规定）——此项**就地登记**于本节（§1.5.4「未指定行为」条目内），计划在后续 **P0-2** 收紧为左到右逐产生式求值（**不**入附录 B——附录 B 作用域声明为「编译器实现层遗留工程决断、非语言语义」，而求值顺序属语言语义的行为分类范畴，入附录 B 会与该作用域声明冲突）。**另一项未指定登记**：**资源耗尽（栈溢出 / OOM）与 double-panic** 亦归未指定（见下注——规范对其结果不施加唯一约束、不要求文档化，具体终止语义留给后续运行期错误模型 RFC（P1+，待立项）收紧）。

> 注：`panic`（§17，含 `unwrap` / `assert`、debug 整数溢出、除零、越界、约束违约）是**定义行为**（确定性栈展开 + 同序 Drop），**不是** UB。此澄清对齐 §89 #3（panic 展开边界）与 §90 #4（panic 跨 FFI → abort）。

> 注（资源与展开失败——归入未指定行为，终止语义留后续运行期错误模型 RFC P1+）：**栈溢出 / OOM**（资源耗尽）与 **double-panic**（panic 展开过程中 `Drop` / `Resource.release` 再次触发 panic）属**运行期 / 环境层**条件——规范对其结果不施加唯一约束、亦不要求实现文档化其选择，故二者**归入本节「未指定行为」类别**（与「表达式求值顺序」并列登记，见上）：允许若干合规结果之一，具体终止语义（abort / 未指定 / 等）**留给后续运行期错误模型 RFC（P1+，待立项）收紧**——本治理 RFC 仅登记为未指定、不预设具体结果。**二者均非 UB**：UB 的成员准则穷尽为「仅 ① `unsafe` 块 §25.1 / ② `extern` FFI 边界 §90 #4」（见上 UB 条目），资源耗尽不属任一 UB 源——与「未指定行为 ≠ UB」一致（未指定是允许若干合规结果之一的**非 UB 行为**范畴，非「无约束」的 UB）。「安全代码永不 UB」指安全代码不触发 ①② UB 源，**不**就资源边界行为的结果是否唯一确定作承诺（资源边界行为归未指定，见上）——故合规实现在资源耗尽时可产生不同（未指定）结果，此为未指定行为的本质、非合规缺口。冻结 §17 当前承诺 panic 不传染进程（FFI 为唯一例外 §90 #4）；double-panic 是否构成该模型的边界扩展属 **后续运行期错误模型 RFC（P1+，待立项）对 §17 的审查范围**，不在本 RFC 决断——本 RFC **不**开 §17 之外的新例外。

---

## 7. build profile（debug / release）

落地形态：规范新增 **§1.5.5**。**填补 synthesis §4 交叉印证 #12（build profile 未定义）。**

构建配置文件（build profile）控制编译期检查的强度与优化级别。合规实现**必须**支持至少两个 profile：

| profile | 语义 |
|---|---|
| **debug**（`ail build` / `ail run` 默认） | 开发期检测层**全开**：整数溢出 → panic `ArithmeticOverflow`（§90 #1）；契约 / 约束断言**必须**启用（§20 / §15.4）；边界检查启用——越界 panic `IndexOutOfBounds`（§17，**无条件**；§90 #4 指 unsafe/FFI 清单，非此）。 |
| **release**（`ail build --release`） | 优化构建：整数算术**二补数回绕**为规范语义（§90 #1），溢出 panic 关闭；**契约 / 约束断言无条件启用**（§92 #2 / §15.4 / §20，**不得**关闭、与 debug 同），不受 profile 调节。 |

- profile 的具体配置项（`opt-level`、`overflow-checks`）属工具链层，**应**在 `ail.toml` 的 `[profile.debug]` / `[profile.release]` 表声明（§30.1 待补，归 P0-2；具体 schema 见 [RFC 0007](./0007-determinism-batch.md) §9——**无 `debug-assertions` 项**，契约 / 约束 / `assert` / 边界检查均为**无条件**运行期检查）。
- **安全性不变量**：无论 profile，安全代码**不得**产生 UB（§6）。profile 仅调节开发期检测与优化级别，**不**改变安全代码的规范性结果（§90 #1 已锁定 release 回绕为规范语义，故溢出定性差异属规范许可、非 profile 引入的不一致）。

> 与 §90 #1 的关系：§90 #1 已分别规定 release 二补数回绕（规范语义）与 debug 溢出 panic（开发期检测层），但未给 profile 本身命名或定义可配置项。本节把 §90 #1 隐式使用的 debug / release 提升为正式 profile 概念，二者完全自洽、无新决断。

---

## 8. Glossary（附录 D 术语表）

落地形态：规范新增 **附录 D**（资料性）。

> 资料性术语索引，给出关键术语的精确定义与 RFC 2119 映射。术语定义与正文冲突时，以正文（尤其 §1.5）为准。

**D.1 规范性词汇（RFC 2119 完整映射，见 §1.5.1 / §4）**：必须 / 须 / 必(MUST / SHALL / REQUIRED；正文主导 MUST 词形「须」「必」为同义、按 MUST 处理，见 §4 正文既有非表词形约束力保留注) / 不得(SHALL NOT / MUST NOT；正文主导禁止词形「禁 / 禁止 / 不可 / 不能」为同义、按 MUST NOT 处理，见 §4 正文既有非表词形约束力保留注) / 应(SHOULD / RECOMMENDED) / 不应(SHOULD NOT / NOT RECOMMENDED) / 可以(MAY / OPTIONAL)，含义务定义——此为 §4（§1.5.1）5 行义务等级表的**完整术语映射**（MUST 类已覆盖正文实际使用的「必须 / 须 / 必」全部词形、禁止类已覆盖「不得 / 禁 / 禁止 / 不可 / 不能」全部词形），满足 §4 正文「附录 D 给出完整术语映射」的要求。

**D.2 语言与类型术语**：语义类型（semantic type，§15.3）、约束（constraint，§15.4 / §92 #2 / §92 #6）、`meaning`（§15.3 / §8.5 / §92 #8）、`effect` / 效果系统（§19）、trait（§15.11 / §86 #10，泛型 bound→编译期单态化、可带默认实现）、interface（§15.11 / §86 #10，dyn 派发→方法禁泛型、值为 Move 句柄——trait vs interface 类型层二分）、`unsafe` 块（§25.1 精确清单，安全代码的对立面）、`extern` / FFI（§25.3，调用须置于 `unsafe` 块 §25.1 ②）、契约 contract（§20 前置 `requires` / 后置 `ensures`，与 §15.4 约束类型 constraint 显式区分）、所有权 ownership（§18）、move（§18.3）、borrow / borrow_mut（§18.5 / §74.2）、copy（§18.3 / §18.4，Copy 与 COW 类型的显式克隆；Move-only 资源类型不可 `copy`）、Drop / Resource.release（§18.7，确定性析构，与 §6「析构顺序」定义行为范例对齐）、Send / Sync（§18.6 / §87 #6，编译器自动派生的 marker trait、跨线程安全边界，「安全代码无数据竞争」的静态执行机制）、Task（§21 / §87）、Actor（§21 / §87 #5）、`.ailmeta`（§24 / §32）、`schema_version`（当前 `0.2.0`，RFC 0001–0004 预留 `0.3.0`，§88 #2）。

**D.3 规范工程术语（见 §1.5）**：合规实现 conforming implementation（§1.5.3）、normative / informative（§1.5.2）、定义行为 / UB / 实现定义 / 未指定（§1.5.4）、安全代码 safe code（§1.5.3 / §6，= 不含 `unsafe` 块的程序）、build profile（§1.5.5）、panic（§17，非 UB）。

> 完整条目定义见落地时的附录 D 正文（本 RFC §3–§7 已含 **D.1 规范性词汇与 D.3 治理术语**的全部精确表述；**D.2 语言与类型术语**的权威定义见各条目所引冻结章节——语言/标准库正文 §8–§42 + 决议记录 §83–§94，个别条目辅引 Part IV §74 资料性实现细节，附录 D 为其统一索引化）。

---

## 9. RFC lifecycle（独立文档）

落地形态：规范引用的**独立文档** `docs/rfc-process.md`（不进 AILANG.md 正文，因其定义 RFC 自身的流程，作为独立治理基础设施更合适）。

定义 AILang 设计提案（RFC）从 **Draft → Review → FCP → Accepted → Implemented** 的生命周期，含：

- **状态机**：Draft（起草）/ Review（公开 + 对抗式 workflow 审查）/ FCP（最终评论期）/ Accepted（采纳待实现）/ Implemented（合并进规范或编译器）/ Rejected / Postponed / Withdrawn。
- **编号与文件**：连续四位 `NNNN`，路径 `docs/rfc/NNNN-kebab-title.md`；头部**必须**含标题、状态、版本（Draft vN）、关联 P0/P1 分级、是否触动冻结规范、是否新增关键字 / 文法产生式。
- **采纳标准**：自洽（与 §1.5 / §17 / §90 无矛盾）+ 可对抗验证（收敛 0H/0M/0L）+ 冻结影响声明 + 实现可行性。
- **规范解冻通道**：对冻结规范的直接修改**应**经一份 Accepted RFC 授权；或属 P0-0 类治理地基补齐（不改语义决断、不增关键字 / 产生式）并显式登记于附录 A、版本号维持不变。

> 历史决议记录（§83–§94，记录 I–XII）是本流程建立前的既有沉淀，其权威性不受影响。RFC 0001–0004（Draft）在本流程下状态不变。

---

## 10. 不变量与自洽核查

本 RFC 的全部行为分类与 profile 定义**必须**与既有冻结决议对齐。下表为合并前的自洽核查清单：

| 本 RFC 条款 | 对齐的冻结决议 | 自洽性 |
|---|---|---|
| §6 UB 仅来自 unsafe/extern | §25.1 精确清单 + §25.3 FFI + §90 #4 | ✅ 边界一致，无新决断 |
| §6 panic 是定义行为非 UB | §17 确定性栈展开 + §89 #3 | ✅ 一致 |
| §6 栈溢出/OOM + double-panic = 归入未指定行为（与求值序并列登记）、非 UB（成员准则仅 ①②）、终止语义留后续运行期错误模型 RFC（P1+） | §25.1 / §90 #4（UB 成员准则源）/ §17（不传染进程，FFI 唯一例外） | ✅ 守治理边界（不开 §17 外新例外；消除「不在覆盖范围」与「非 UB」的旧措辞矛盾，UB 后果性质 vs 成员准则已显式区分；终止语义归属已由 §13 OQ #4 登记、不再指向已声明 out-of-scope 的 P0-3/RFC 0008） |
| §7 debug 溢出 panic / release 回绕 | §90 #1 | ✅ 完全对齐（§7 仅提升为 profile 概念） |
| §7 契约/约束断言无条件（debug+release 均 MUST 启用、无 debug-assertions 项） | §92 #2 / §15.4 / §20 / §89 #3 | ✅ 与 §17 panic 表（仅溢出标 debug）一致 |
| §7 安全代码不产生 UB | §90 立场「永不 UB」 | ✅ 形式化既有立场 |
| §6 Drop 声明序属定义行为 | §18.7 / §90 #5 | ✅ 一致 |
| §5 标签全集归类（9 类内容标签 + `[矛盾]`/`[决断]` 修饰类不参与判定 + 复合采 `[A/B]` 斜杠形 + 规范性闭集声明） | §83–§94 既有标签（grep 复核：22 种相异**决议标注**全归类；`#[lang]` 为编译器属性、不计入） | ✅ 全集已穷举（含 `[矛盾]`/`[决断]`，复合示例改斜杠形） |
| **§3 MUST/SHOULD 来源的 normative 一致性** | §3 引 Part I（§27/§15/§18/§19/§20/§22/§24）/ Part II §32 / RFC 0008 §4 等（非穷举）；§69.3/§78 仅资料性参考 | ✅ 已核查：§3 无 MUST/SHOULD 以 Part IV informative 为权威（与 §5 分区一致；§32 归 Part II §28–§42 规范性核心、§24 归 Part I——Part 标签已显式分列） |
| 全部交叉引用章节号 | §15.1/§15.4/§17/§18.7/§20/§24/§25.1/§25.3/§90（normative）；§69.3/§74/§74.2/§78（informative 参考） | ✅ grep 核查零悬空 + 已区分 normative/informative |

**零新关键字核查**：`must`/`shall`/`should`/`may`/`conformance`/`normative`/`informative`/`profile`/`defined`/`undefined` 在 §9（56 关键字表）中均不出现——本 RFC 不引入任何关键字。**零产生式核查**：不触动 §27 任一产生式。

---

## 11. 落地映射（合并进 AILANG.md 的位置）

当本 RFC 经 review 收敛并 Accepted 后，合并进 `docs/AILANG.md` 的位置：

| 本 RFC 内容 | 落地位置 | 性质 |
|---|---|---|
| §3 Conformance + §4 词汇 + §5 分区 + §6 四分法 + §7 build profile | 新增 **§1.5**（插在 §1 摘要后、§2 前） | 规范性（Part I 元规则） |
| §8 Glossary | 新增 **附录 D** | 资料性 |
| §9 RFC lifecycle | 独立 **`docs/rfc-process.md`**（§1.5 引用） | 治理基础设施 |
| 求值顺序未指定登记 | **§1.5.4「未指定行为」条目就地登记**（P0-2 收紧为左到右；不入附录 B——附录 B 为「非语言语义」工程层，求值序属语义分类） | 登记现状 |
| 本次合并记录 | **附录 A** 演进记录（条目续编） | 版本号维持 v0.2.1 |

目录同步：Part 0 行加「§1.5 Conformance 与规范性词汇（规范层）」；附录行加「D 术语表 Glossary」。

---

## 12. 与既有决议的关系 / handoff

- **不触动 §83–§94 任一冻结决议**——本 RFC 是元规则层，不重新讨论任何语义决断。§90 #1/#4/#5、§17、§25.1/§25.3 等均作为**既定前提**被引用，不被修改。
- **与 RFC 0001–0004 正交**——四份特性 RFC 均声明「不触动冻结规范、零新关键字」，本 RFC 为它们提供**规范性约束力的形式化载体**（conformance + RFC 2119）。合并本 RFC 后，0001–0004 的条款在 Implemented 时可被明确标注 MUST / SHOULD。
- **解锁 P0-1～P0-4**——synthesis §6 的后续 P0（形式化层 / 确定性条款 / 诊断协议 / FFI）在补齐时，每条 typing rule、每条求值顺序决断都可落在 §1.5 提供的规范性词汇框架内（MUST / SHOULD / MAY）。
- **schema_version 无关**——本 RFC 不改 `.ailmeta` schema（§88 #2 的 `0.2.0` → `0.3.0` 升级由 RFC 0001–0004 的 `examples[]`/`contracts`/`properties[]` rider 承担，与本 RFC 无关）。

---

## 13. 开放问题

1. **求值顺序收紧时机**——§6 登记「表达式求值顺序未指定」为遗留，计划 P0-2 收紧为左到右。本 RFC 仅登记现状，**不**预先决断收紧方向（留 P0-2 单独 RFC 或决断）。
2. **`ail.toml` profile 表**——§7 指明 profile 配置项属工具链层、归 §30.1（P0-2）。本 RFC 不定义具体配置项 schema。
3. **实现定义行为登记表**——设计目标 5 声明 v0.2.1 未登记任何实现定义行为；未来引入时的附录登记格式待定（新增附录 E 或等价的语言语义层登记位）。实现定义行为属 §6 语言语义分类（规范要求实现文档化其选择），**不入附录 B**——附录 B 作用域为「编译器实现层遗留工程决断、非语言语义」（同 §6 求值序论证）。
4. **资源耗尽 / double-panic 终止语义归属**——§6 把「栈溢出 / OOM + double-panic」登记为未指定行为，其具体终止语义（abort / 未指定 / 等）与 double-panic 是否构成 §17 panic 模型的边界扩展，留给**后续运行期错误模型 RFC（P1+，待立项）**收紧——**不**归已收尾的 P0-3（[RFC 0008](./0008-diagnostics-taskhandle.md)，主题为诊断协议 / 错误码 / TaskHandle 错误态，§10 OQ #7 显式声明进程级资源耗尽终止语义 out-of-scope、改推 P1+）。本登记消除 §6 旧版「留 P0-3 收紧」对已声明 out-of-scope 目标的悬空引用。

---

## 14. 收敛轨迹

**收敛轨迹**：已跑多轮对抗式 workflow（pass-2 → pass-14，与 RFC 0006/0007 同批 pipeline，每轮修正后 FRESH 重跑、无 `resumeFromRunId`）。历轮累计修正：pass-3 H1 §3 Conformance 引用重定向到 normative 来源（MUST/SHOULD 仅引 normative）；pass-13 §5 增「无标签条目兜底」条款（§83 决议记录 I 10 项无标签条目视作资料性索引、规范性权威由指针承载，闭合标签分类盲区）；其余多轮 M/L（行为四分法 / build profile / RFC 2119 映射 / glossary / normative-informative 分区）。**pass-14 报 0 条 confirmed**（RFC 0005 本 pass clean，自 pass-3 修正后持续收敛、无回归）。post-pass-14 全部历轮 findings 已修正。**pass-15 / pass-16 报 0 confirmed**（持续 clean、无回归）。**pass-17 报 2 confirmed [1M/1L] 并已全部修正**：① [L] §3 引用纪律注把 §32（实属 Part II、§28–§42 规范性核心）误归「Part I 含」、与 §10/§5 Part 归类不一致 → 改 §3 分列形式「Part I（…§24）、Part II §32」+ §10 自洽行同步分列（§24 归 Part I、§32 归 Part II）；② [M] D.1「完整术语映射」遗漏正文实际主导的禁止词形「禁/禁止/不可」（冻结正文禁/禁止 60 处、不可 34 处、不能 5 处、不得仅 1 处 §15.11）→ §4 表「不得」行扩为「不得/禁/禁止/不可」+ §4 加「正文既有禁止词形约束力保留」注（粗体规则适用对象切割为 §4 表内新关键词、冻结正文禁/禁止/不可不因未加粗丧失约束力、规范地位由 §5 Part I normative 分区承载）+ D.1 同步标注。post-pass-17 待 pass-18 复验收敛。**pass-18 报 1 confirmed [1M] 并已修正**：① [M] D.1「完整术语映射」的 **MUST 类**遗漏正文实际主导词形「须」（独立「须」73 处，远多于「必须」14 处、为 MUST 类绝对主导词形；与 pass-17 已修的禁止类遗漏完全同构且量级相当）→ 与 pass-17 对称修复四件套：§4 line22 词汇审计补「须 73 处」、§4 line70 表 MUST 行扩为「**必须** / 须」+ 下注、§4 line80 约束力保留注由「禁止词形」扩为「非表词形」对称覆盖 MUST 类「须」+ 禁止类「禁/禁止/不可」（并注明「需」46 处描述义/「只能」5 处能力义不映射）、D.1 line155 MUST 条目补「须」+ 括注扩为「MUST 类已覆盖...全部词形」。post-pass-18 待 pass-19 复验收敛。**pass-19 报 4 confirmed [0H/2M/2L] 并已全部修正**：① [M] selfconsistency——§6 line123/127 + §10 line188 + 注标题 5 处把「栈溢出/OOM + double-panic」终止语义收紧「留给 P0-3 错误模型 RFC」，但被指向的 RFC 0008（=P0-3）§10 OQ#7 显式声明进程级资源耗尽终止语义 out-of-scope、改推 P1+——属悬空引用 → 5 处「P0-3」统一改为「后续运行期错误模型 RFC（P1+，待立项）」+ §13 新增 OQ #4 登记归属、消除指向 out-of-scope 目标的悬空；② [L] xref-dangling——§4 line82「须」示例列表「§15『须 `as` 消歧』」「§18『append 须声明 borrow_mut』」章节号归属错误（`as` 消歧实为 §12.2 line453、append borrow_mut 实为 §15.5 line570）→ 重新分组「§12 …『须 `as` 消歧』 / §15 …『append 须声明 borrow_mut』」；③ [L] normative-split——§5 line104 复合标签示例 `[语义/示例]` 在冻结规范 0 命中（实际为反序 `[示例/语义]` §85 #5 line3260）→ 改示例为 `[示例/语义]`；④ [M] glossary-completeness——D.1「完整术语映射」禁止类遗漏正文实际词形「不能」（grep 实测 5 处：Part I 安全不变量禁止义 §18.5/§21.2 + Part III 教程注释 §48 + Part IV 模块设计/决议汇总复述 §74.2/§81——与 pass-17「禁/禁止/不可」、pass-18「须」遗漏完全同构）→ 对称修复：§1 line22 词汇审计补「不能 5 处」+ §4 line71 表禁止行扩为「不得/禁/禁止/不可/不能」+ §4 line85 约束力保留注禁止类列举扩入「不能」并补 3 处正文示例 + D.1 line155 括注禁止类列举与 inline 主导词形同步扩入「不能」+ §14 line236 grep 范围补「不能 5 处」。post-pass-19 待 pass-20 复验。**pass-20 报 3 confirmed [0H/0M/3L] 并已全部修正**（frozen-boundary / rfc2119-mapping / glossary-completeness 三维度同构命中同一根因）：① [L] §4 line83 禁止类约束力保留注把 §81「struct 字段不能存引用」与 §18.5/§21.2 并列归纳为「均 Part I 规范性强制条款」，但 §81 实属 Part IV（§65–§82、§5 划为 informative 不构成合规要求），且「不能」全文 5 处经 grep 实测仅 2 处（§18.5 line846 borrow_mut 独占、§21.2 line1085 spawn 数据）真属 Part I，余 §48（Part III 教程注释 line2133）/§74.2（Part IV 模块设计 line3021）/§81（Part IV 决议汇总 line3175）为各自对应不同禁止不变量的 Part III/IV 资料性复述、规范权威分别回指 Part I 源头条款（§48 名义类型禁隐式转换→§15.3 / §84 #1；§74.2 borrow_mut 独占→§18.5 line846；§81 struct 字段禁存引用→§18.5 line848） → §4 line83 去除 §81 内联枚举、改精确 Part 归属注（余 3 处标 Part III/IV 资料性复述、回指各自 Part I 源头）+ §14 pass-19 note ④ 同步纠正原「均 Part I」误述为准确 Part 分布。post-pass-20 待 pass-21 复验收敛。**pass-21 报 3 confirmed [0H/1M/2L] 并已全部修正**（selfconsistency / frozen-boundary / glossary-completeness 三维度同构命中同一根因）：pass-20 对 §81 Part 归属的修正虽在 §14 trajectory 分列了 3 处的 Part I 源头，但 §4 line83 normative 注 + §14 pass-20 note 的措辞仍把余 3 处「不能」（§48 / §74.2 / §81）统一标「同一禁止不变量」回指 §18.5 line848，与上分列自相矛盾、且与 §4 line83 inline 枚举原引 §18.5 borrow_mut（line846）作 Part I 示例冲突（§74.2 同一 borrow_mut 不变量被重指 line848）。经 grep + 逐行核对三处分属三个不同禁止不变量——① §48 line2133「不能隐式转 UserId（名义不同）」为 §15.3 名义类型 / §84 #1 字面量强制（**名义类型禁隐式转换**不变量、非借用族）；② §74.2 line3021「borrow_mut 独占...不能与只读借用并存」为 §18.5 line846（**borrow_mut 独占**不变量、与 §4 line83 inline 枚举同一 bullet）；③ §81 line3175「struct 字段不能存引用」为 §18.5 line848（**不逃逸：禁止存入 struct 字段**）。修正=§4 line83 inline 枚举保留真实 Part I 示例（line846 borrow_mut + §21.2 spawn）、余 3 处「不能」改按各自实际不变量分别回指 Part I 源头（§48→§15.3/§84#1、§74.2→line846、§81→line848、三者分属三个不同禁止不变量、均非独立 Part I 强制条款）+ §14 pass-20 note ① 同步纠正原「同一禁止不变量」误述为三者分属。post-pass-21 待 pass-22 复验收敛。**pass-22 报 2 confirmed [0H/0M/2L] 并已全部修正**（rfc2119-mapping / glossary-completeness 两维度同构命中同一根因——pass-19 对禁止类词形列表的对称修复遗留两处 sync 盲点）：① [L] rfc2119-mapping——pass-19 把「不能」扩入禁止类列举时同步了 §4 line71 表 + line83 约束力保留注 + line155 D.1 + §1 line22 词汇审计四处，但 §4 line85 **摘要**禁止类列举（`冻结正文既有的「须」「禁 / 禁止 / 不可」不因未加粗而丧失约束力`）漏扩「不能」→ 补入「不能」为 4 词形 `禁 / 禁止 / 不可 / 不能`、与 line71/83/155 三处统一；② [L] glossary-completeness——§4 line82 MUST 类「须」示例列表含「§25『extern 须置于 unsafe 块』」，但冻结 §25.1 line1378 把 extern/unsafe 归入 **「必需操作」精确清单**（用「必需」、属 line85 已排除的「需/必需」描述义族）、并非以「须」措辞表达，词形错挂且与 line85 排除规则自相矛盾 → 换 §25.1 真实「须」条款「全局可变须走 `Mutex<T>`/`Shared<T>`」（line1378 同行、`static mut` → Mutex/Shared 的真实 MUST 措辞）。post-pass-22 待 pass-23 复验收敛。审查维度：自洽性 / 冻结边界 / 交叉引用悬空 / 与 §90 §17 §25 对齐 / RFC 2119 映射准确性 / glossary 完备性 / normative-informative 划分正确性 / 行为四分法 soundness。

> 由于本 RFC 是元规则层（无代码、无文法、无算法），验证重心在**规范文本自洽与交叉引用完整性**，而非 0001–0004 的实现可行性——这降低了验证复杂度，预期收敛轮次少于特性 RFC。

---

*本 RFC 由综合判断 [`synthesis-2026-07.md`](../research/synthesis-2026-07.md) §6 P0-0 驱动。落地后将使 AILang 规范获得「conformance / RFC 2119 / 行为分类」的治理三件套，把 spec-maturity 评分从「约当 Rust 2010–2012」向成熟规范推进，并为 P0-1～P0-4 的每一条补齐提供规范性约束力载体。*
